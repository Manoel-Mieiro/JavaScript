let decision = window.confirm("Is it OK?");
console.log(decision); //If input = ok, true; else, false
//-----------------------------------------------------------------//
    let remove = confirm("Remove all data?");
    let message = remove ? "Deleting Data" : "Cancelled" 

    console.log(message) ;
